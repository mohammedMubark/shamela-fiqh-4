# dependencies
node_modules/

# build output
dist/
java/classes/
java/test-classes/
.lucene-build/
*.jar

# MCPB packages
*.mcpb

# generated test fixtures and every artefact the test run produces
tests/fixtures/generated/
tests/fixtures/.index/
tests/fixtures/.index-lucene/
tests/fixtures/.out/
tests/fixtures/.out-smoke/
.fixtures/

# search index + exports (never committed, never inside the Shamela folder)
.index/
exports/
bench-out/

# Shamela data must never be committed
*.db
*.db-shm
*.db-wal
*.bok
master.db

# diagnostic output produced on a user's machine, not source
verify.json
schema*.txt
lucene*.txt
inspect*.txt

# local env
.env
.env.local

# editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
